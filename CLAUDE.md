# ProgateHackathon2026-08

Progate ハッカソン向けのチーム開発プロジェクト。フロントエンドは Flutter (`frontend/`)、バックエンドは Supabase を利用。

## 技術スタック

- **フロントエンド**: Flutter (`frontend/`)。状態管理・画面は `lib/screens/` 配下。
- **バックエンド**: Supabase (`frontend/lib/main.dart` で URL/anonKey を初期化済み)。専用の `backend/` ディレクトリは無し。
- 依存: `flutter_foreground_task`, `sensors_plus`(シェイク検知), `supabase_flutter`, `shared_preferences`

## チーム体制・担当

- コードは複数人で分担して開発している。ブランチ名からも分かる通り、画面ごとに担当が分かれている(例: `album`, `homedegin`, `shake_movement`, `basic_screens` など)。
- 自分が触っている担当外の画面/機能を推測で変更しない。関係しそうな場合は一言確認してから進める。

## AI(Claude Code)の振る舞いルール

このリポジトリで作業する際は以下を守ること。「変更しない」ではなく「確認してから、わかりやすく進める」がポイント。

1. **コードを変更する前に、必ずどこを・なぜ・どう変えるかを先に説明し、実行してよいか確認を取ってから Edit/Write する。** 提案だけして終わりにせず、確認が取れたら手を動かして実装まで進める。
   - 例外: Supabase の URL/anonKey のような単純な設定値の埋め込みなど、手作業がミスを誘発しやすい機械的な作業は確認なしで代行してよい。
2. **変更内容は分かりやすく説明する。** 触ったファイル・変更箇所・理由を、専門用語をかみ砕きながら簡潔に伝える。大規模なリファクタや構成変更は特に丁寧に説明してから進める。
3. **コードの意味を軽く教える。** 変更を説明するとき、関連するコードが何をしているか(学習中のメンバー向けに)一言添える。聞かれてもいないのに長い解説をする必要はない、あくまで軽く。
4. **git の破壊的操作**(`push --force`, `reset --hard`, ブランチ削除など)や、**他人のブランチへの直接コミット**は必ず事前確認する。
5. 開発初心者のメンバーもいるチームなので、専門用語を使うときは簡潔に補足する。起動・操作方法で迷ったら `/get-started` スキルを参照する。
6. 未確認の前提でコードや設計を断定しない。分からないことがあれば実際のコード・設定を確認してから答える。
